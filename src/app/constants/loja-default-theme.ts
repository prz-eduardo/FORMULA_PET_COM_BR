/**
 * Cores padrão da loja (fallback antes do tema dinâmico).
 * Espelhar em styles.scss :root — comentário de sync no SCSS.
 */
export const LOJA_DEFAULT_THEME_COLORS = {
  primary: '#b45309',
  accent: '#f5a700',
  surface: '#ffffff',
} as const;
