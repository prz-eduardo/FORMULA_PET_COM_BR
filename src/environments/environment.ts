export const environment = {
  production: false,
  /** Quando true, o site exibe apenas a página “em breve” (build `com-br` para o domínio .com.br). */
  comingSoon: false,
  // apiBaseUrl : 'http://localhost:8080'
  apiBaseUrl: 'https://petspherecombrbackend-production.up.railway.app' // substitua pelo URL real do deploy (Railway)
};
